# Konfigurasi untuk Drupal 11 (Port 80)
server {
    listen 80;
    server_name localhost;
    root /var/www/html/drupal11/web; # Sesuaikan dengan volume di compose

    index index.php;

    location / {
        try_files $uri /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        # Menghubungkan ke service drupal_fpm
        fastcgi_pass drupal_fpm:9000; 
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires max;
        log_not_found off;
    }
}

# Konfigurasi untuk Drupal 8 (Port 81)
server {
    listen 81;
    server_name localhost;
    root /var/www/html2/drupal8/web; # Sesuaikan dengan volume di compose

    index index.php;

    location / {
        try_files $uri /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        # Menghubungkan ke service drupal_fpm7
        fastcgi_pass drupal_fpm7:9000; 
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires max;
        log_not_found off;
    }
}